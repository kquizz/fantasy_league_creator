<p style="color: green"><%= notice %></p>

<%= render @show %>

<div>
  <%= link_to "Edit this show", edit_show_path(@show) %> |
  <%= link_to "Back to shows", shows_path %>
<br/><br/>
  <%= button_to "Destroy this show", @show, method: :delete, class: "destroy-btn" %>
</div>
